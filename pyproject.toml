[project]
name = "codex-capability-hub"
version = "0.1.0"
description = "Lazy capability routing for Codex skills, MCP servers, and plugins"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["scripts"]
